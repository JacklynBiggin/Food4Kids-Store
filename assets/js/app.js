function addToCart(food,price,img_url) {
    // Create the image
    $( "#bag-items" ).append( '<img src="' + img_url + '" class="animated fadeInDown bag-item">');
    // Add to cart list

    // Modify cart price?
}